# C Program Boilerplate (Starter Template)

This repository/file provides the minimal, standard C boilerplate code required to compile and execute a basic program. All C programs begin execution inside the `main` function.

---

## The Boilerplate Code

```c
#include <stdio.h>

int main() {
    // Basic output function included from stdio.h
    printf("Hello, C World!\n"); 
    
    return 0;
}
